nvm use v8.9.4

to deploy contract:
truffle migrate

need to get the contract address into app - server and client

To run server:
cd app
npm install
npm start

browser:

localhost:3001


ganache
import user into metamask
